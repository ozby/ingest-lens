name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  GH_PACKAGES_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}
  NODE_AUTH_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  token-gate:
    name: Detect package token
    runs-on: ubuntu-latest
    outputs:
      has_packages_token: ${{ steps.detect.outputs.has_packages_token }}
    steps:
      - name: Detect GH_PACKAGES_TOKEN presence
        id: detect
        env:
          GH_PACKAGES_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}
        run: |
          if [ -n "${GH_PACKAGES_TOKEN}" ]; then
            echo "has_packages_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_packages_token=false" >> "$GITHUB_OUTPUT"
          fi

  setup:
    name: Install
    if: ${{ needs.token-gate.outputs.has_packages_token == 'true' }}
    needs: token-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-monorepo

  check:
    name: Check (lint + types)
    if: ${{ needs.token-gate.outputs.has_packages_token == 'true' }}
    runs-on: ubuntu-latest
    needs: [token-gate, setup]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-monorepo
      - run: pnpm check

  test:
    name: Test
    if: ${{ needs.token-gate.outputs.has_packages_token == 'true' }}
    runs-on: ubuntu-latest
    needs: [token-gate, setup]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-monorepo
      - run: pnpm test

  catalog-drift:
    name: Catalog drift
    if: ${{ needs.token-gate.outputs.has_packages_token == 'true' }}
    runs-on: ubuntu-latest
    needs: [token-gate, setup]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-monorepo
      - run: pnpm catalog:check

  blueprint-validate:
    name: Blueprint validation
    if: ${{ needs.token-gate.outputs.has_packages_token == 'true' }}
    runs-on: ubuntu-latest
    needs: [token-gate, setup]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-monorepo
      - run: pnpm blueprints:check

  lore-commits:
    name: Validate Lore commits
    if: ${{ needs.token-gate.outputs.has_packages_token == 'true' }}
    runs-on: ubuntu-latest
    needs: [token-gate, setup]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - name: Validate Lore commits
        run: |
          BASE=${{ github.base_ref || 'main' }}
          git fetch origin "$BASE" --depth=50 2>/dev/null || true
          COMMITS=$(git log "origin/$BASE..HEAD" --format="%H" 2>/dev/null || true)
          if [ -z "$COMMITS" ]; then
            echo "No branch commits to validate."
            exit 0
          fi
          FAIL=0
          for SHA in $COMMITS; do
            git log -1 --format="%B" "$SHA" > /tmp/commit-msg.txt
            bun ./scripts/run-webpresso-cli.ts agent audit commit-message --lore-warn --message-file /tmp/commit-msg.txt || FAIL=1
          done
          exit $FAIL

  mutation:
    name: Mutation (affected)
    if: ${{ github.event_name == 'pull_request' && needs.token-gate.outputs.has_packages_token == 'true' }}
    runs-on: ubuntu-latest
    needs: [token-gate, test]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - run: bun ./scripts/affected-mutation.ts
