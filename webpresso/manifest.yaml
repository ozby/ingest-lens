apiVersion: webpresso.dev/v1
entry: apps/client/src/main.tsx
auth:
  basePath: /auth
  providers:
    - type: email
    - type: google
      clientId: GOOGLE_CLIENT_ID
  cookieDomain: .ingest-lens.ozby.dev
build:
  outDir: dist
