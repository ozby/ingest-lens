{
  "name": "ozbys-node-pubsub",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "catalog:check": "bun ./scripts/check-catalog-drift.ts",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check-types": "turbo run check-types",
    "prepare": "husky"
  },
  "devDependencies": {
    "prettier": "catalog:tooling",
    "turbo": "^2.4.4",
    "husky": "catalog:"
  },
  "packageManager": "pnpm@9.15.2",
  "engines": {
    "node": ">=18"
  }
}
